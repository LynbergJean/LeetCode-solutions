#include<stdio.h>
#include<string.h>

//given a string create a new word using the first letter of each word in the string
void read_line
int main(){
  int n;
  scanf("%d",&n);

  char arr1[100];
  
  int i = 0;
  while(i < n){
    
  }
}
